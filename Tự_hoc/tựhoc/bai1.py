# python object
# trong python tất cả mọi thứ đều là một đối tượng (object). Kiểu chuỗi(string), kiểu số(numbers) và kiểu logic (booleans), cũng được coi là một đối tượng
# Đối tượng (object) được xem như một ô chứa dữ liệu bên trong
# Kiểu số (number), kiểu chuỗi(string) và kiểu logic (boolean) chỉ chứa được 1 giá trị duy nhất
# Tuples, list, set, dictionaries có thể chứa được 1  hoặc nhiều giá trị